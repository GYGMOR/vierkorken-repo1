const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const tickets = await prisma.eventTicket.findMany({ include: { event: { select: { title: true } }, user: { select: { email: true, firstName: true, lastName: true } } } }); console.log('📊 Tickets in der Datenbank:'); console.log('Anzahl:', tickets.length); tickets.forEach((t, i) => { console.log(\); console.log('   QR-Code:', t.qrCode); console.log('   Event:', t.event.title); console.log('   User:', t.user.firstName, t.user.lastName); console.log('   Status:', t.status); }); await prisma.\(); } main().catch(console.error);
